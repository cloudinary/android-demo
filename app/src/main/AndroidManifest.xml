<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cloudinary.android.demo">

    <application
        android:name=".app.MainApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".app.MainActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".app.UploadActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.Translucent.lightStatus.upload" />
        <activity
            android:name=".app.CategoriesActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.Translucent.lightStatus" />
        <activity
            android:name=".app.ProductsActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.Translucent.darkStatus" />
        <activity
            android:name=".app.ProductActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.lightStatus" />
    </application>

</manifest>