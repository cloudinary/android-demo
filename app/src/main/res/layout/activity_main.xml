<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/main_toolbar"
        android:layout_width="match_parent"
        android:layout_height="?android:actionBarSize"
        android:background="@color/colorPrimary"
        android:theme="@style/AppToolbar">

        <TextView
            android:id="@+id/main_text_title"
            style="@style/TextAppearance.AppCompat.Widget.ActionBar.Title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/colorPrimary"
            android:ellipsize="end"
            android:fontFamily="@font/montserrat_alternates_medium_600"
            android:lines="1"
            android:textAlignment="center"
            android:textSize="@dimen/collapsed_title_font_size"
            app:fontFamily="@font/montserrat_alternates_medium_600" />
    </androidx.appcompat.widget.Toolbar>

    <FrameLayout
        android:id="@+id/category_viewgroup_container"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/main_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorPrimary"
        app:itemIconTint="@drawable/color_navigation_color_list"
        app:itemTextColor="@color/colorAccent"
        app:menu="@menu/mnu_navigation" />
</LinearLayout>